# 化妆品文章智能检索系统 — 产品设计文档 v3.1

> **版本**: v3.1
> **日期**: 2026-08-25
> **状态**: 已上线运行
> **前置版本**: v3.0（2026-08-24）、v2.0（2026-08-20）、v1.2（2026-06-17）

---

## 一、产品定位

面向化妆品行业从业者的垂直文章智能检索平台，从"手动检索工具"升级为"智能找文+报告生成"双功能产品。

### 核心能力
- **智能找文**：自然语言提问 → 意图锚定检索 → 精准推荐文章
- **智能选文报告**：勾选文章 → 设置报告要求 → 人工生成交付
- **定制化深度报告**：开放式需求描述 → 后台智能辅助人工处理

---

## 二、当前状态

### 2.1 数据基线

| 指标 | 数值 |
|------|------|
| 数据库文章 | **1,262 篇**（已去重） |
| 公众号覆盖 | **18 个**（白名单） |
| 数据版本 | **v76** |
| 摘要/关键词 | **1,261 篇已生成**（99.9%），1篇空，5篇永久不可用 |
| 日期范围 | 2026-06-15 ~ 2026-08-24 |

### 2.2 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                        用户浏览器                              │
└──────────────────┬──────────────────┬───────────────────────┘
                   │                  │
       https://www.cosmetic-search.com   https://api.cosmetic-search.com
                   │                  │
                   ▼                  ▼
       ┌───────────────────┐  ┌──────────────────────────────┐
       │   Vercel (前端)   │  │ 阿里云ECS 8.133.238.76 (后端) │
       │ 5个HTML页面       │  │ nginx → FastAPI + SQLite     │
       │ data.vXX.json     │  │ Let's Encrypt HTTPS证书      │
       └───────────────────┘  └──────────────────────────────┘
                                          │
                                          ▼
                                ┌─────────────────────────┐
                                │ Mac mini (数据生产端)    │
                                │ RPA采集 → SQLite入库    │
                                │ 每日凌晨4点推送到GitHub   │
                                └─────────────────────────┘
                                          │
                                          ▼
                                     GitHub (中转)
```

### 2.3 部署架构

| 环境 | 地址 | 说明 |
|------|------|------|
| 主站（冻结） | www.cosmetic-search.com | GitHub Pages，main分支v73冻结，旧视觉 |
| 预览站（活跃） | benavidesbraian710-hub.github.io/cosmetic-preview/ | GitHub Pages，feature/new-arch分支 |
| **后端API** | **https://api.cosmetic-search.com** | **阿里云轻量ECS，nginx+FastAPI+HTTPS** |
| 数据库 | 阿里云ECS `/opt/cosmetic-api/data/cosmetic_articles.db` | 每日从Mac mini推送 |

---

## 三、核心创新：意图锚定检索

（详见 v3.0 第三章，本节无变更）

---

## 四、数据层设计

（详见 v3.0 第四章，本节无变更）

---

## 五、API设计

（详见 v3.0 第五章，本节无变更）

新增端点：
- `GET /api/stats` — 统计信息（total_articles, total_sources, date_range）
- `GET /api/health` — 健康检查
- `POST /api/search` — 智能搜索（意图锚定检索）
- `GET /api/articles/{id}` — 文章详情

---

## 六、前端功能

（详见 v3.0 第六章，本节无变更）

---

## 七、采集与入库

### 7.1 采集方式
- **RPA Excel日采**：新电脑RPA每日采集文章链接存入桌面 Excel
- **手动补录**：异构新号/失败文章人工链接兜底

### 7.2 入库流程（已 SOP 化）
1. Nick 经飞书发送 Excel 文件给 AI
2. AI 合并去重 → 与数据库比对净新增
3. 批量抓取入库（反爬失败换手机版UA补抓）
4. 质量校验：日期YYYY-MM-DD截断 + 18号白名单 + 标题非空
5. 导出新版 data.json + data.v{N+1}.json
6. 推送 GitHub 双仓（main + feature/new-arch）
7. Vercel 自动部署

### 7.3 数据版本自动化
- `export_data.py` 自动扫描目录下 `data.vXX.json`，取最大编号+1
- 同时输出 `data.json`（最新）和 `data.v{N+1}.json`（带版本号）
- 前端 URL 加 `?v=YYYYMMDD` 时间戳强制刷新避免 CDN 缓存

---

## 八、技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 前端 | HTML + JavaScript | 无框架，GitHub Pages托管 |
| **后端API** | **FastAPI (Python) + uvicorn** | **阿里云ECS，systemd守护，workers=2** |
| 数据库 | SQLite + FTS5 | 单文件，零外部依赖 |
| LLM | Kimi K3 | 意图解析 + 精排 + 摘要生成 |
| **公网入口** | **HTTPS + 域名 api.cosmetic-search.com** | **Let's Encrypt证书（90天自动续期）** |
| 数据存储 | Supabase | 用户提交的报告需求 |
| **部署** | **阿里云ECS（后端）+ Vercel（前端）** | **GitHub Pages 主站+预览站双仓** |

---

## 九、已规划未实现

| 功能 | 阶段 | 说明 |
|------|------|------|
| 向量检索 | Phase 2 | Embedding + sqlite-vec，覆盖宽泛语义查询 |
| Embedding选型 | Phase 2 | Kimi / OpenAI / 本地模型，待拍板 |
| Chunk切分 | Phase 2 | 固定长度/按段落/按语义，待讨论 |
| SQLite FTS5 全文索引 | 建议中 | 万级文章必需，搜索从5秒降到<500ms |
| uvicorn workers=4 | 建议中 | 2核CPU可承载更多并发 |
| admin后台展示article_reports | 待排期 | 目前需到Supabase后台查看 |
| 小红书评论抓取 | 调研中 | 建议短期继续用八爪鱼 |

---

## 十、决策记录

| 日期 | 决策 | 决策人 |
|------|------|--------|
| 2026-06-17 | 产品设计方案v1.2定稿（FTS5+关键词+LLM精排） | Nick |
| 2026-08-06 | 采集方式从peekaboo切换为RPA | Nick |
| 2026-08-17 | 项目商业化升级（Supabase+五页面） | 老板指示+Nick执行 |
| 2026-08-18 | 双站架构（主站冻结v73+预览站活跃） | Nick |
| 2026-08-19 | 智能选文报告+定制化深度报告双功能上线 | Nick |
| 2026-08-20 | 取消全文入库，改链接+摘要+关键词 | Nick反转 |
| 2026-08-20 | 智能找文功能立项 | Nick提出 |
| 2026-08-21 | 数据库升级两阶段（先关键词摘要，后向量） | Nick拍板 |
| 2026-08-21 | 前端五项设计决策定稿 | Nick拍板 |
| 2026-08-24 | FastAPI+SQLite+Mac mini本地部署 | Nick确认 |
| 2026-08-24 | 三项核心需求（去重/四层分层/K3精排） | Nick拍板 |
| 2026-08-24 | 意图锚定检索替代四层平铺分层 | Nick提出+确认 |
| 2026-08-25 | 后端API独立部署到阿里云ECS | Nick拍板 |
| 2026-08-25 | 固定域名api.cosmetic-search.com（不用Cloudflare） | Nick要求 |
| 2026-08-25 | 数据库同步走 Git pull（不用 rsync） | Nick拍板 |
| 2026-08-25 | 2核1G规格起步（测试阶段），后续可升 | Nick拍板 |

---

## 十一、后端生产环境与定时同步（v3.1新增）

### 11.1 部署架构详解

```
Mac mini (生产端)
  └─ cron: 每日 04:00
      └─ sync_db_to_ecs.sh
          ├─ cp cosmetic_articles.db → api-server/data/
          ├─ git add api-server/data/
          └─ git push origin feature/new-arch

GitHub (中转)
  └─ cosmetic-articles-search 仓库 (feature/new-arch 分支)
      └─ api-server/data/cosmetic_articles.db

阿里云 ECS 8.133.238.76 (消费端)
  └─ cron: 每小时
      └─ cd /opt/cosmetic-api
          └─ git pull origin feature/new-arch
          └─ systemctl restart cosmetic-api
              └─ FastAPI 加载最新数据库
```

### 11.2 ECS 实例规格

| 项 | 值 | 说明 |
|---|---|---|
| 区域 | 华东2（上海） | 离 Nick 近 |
| 镜像 | Ubuntu 22.04 LTS | setup.sh 适配 |
| 规格 | 2核1G / 30GB SSD | 测试阶段够用 |
| 带宽 | 5Mbps 峰值 | API 数据量小 |
| 月费 | 40 元 | 后续按需升级 |
| 公网IP | 8.133.238.76 | 阿里云固定分配 |

### 11.3 服务管理

- **systemd 服务名**：`cosmetic-api.service`
- **工作目录**：`/opt/cosmetic-api`
- **数据库路径**：`/opt/cosmetic-api/data/cosmetic_articles.db`（环境变量 `COSMETIC_DB_PATH` 配置）
- **nginx 反代**：`api.cosmetic-search.com` → `127.0.0.1:8000`
- **HTTPS 证书**：`/etc/letsencrypt/live/api.cosmetic-search.com/`（certbot 自动续期）

### 11.4 部署脚本（api-server/setup.sh）

一键部署脚本内容（详见 `api-server/setup.sh`）：
1. 安装系统依赖（python3, nginx, git, certbot）
2. 克隆代码（从 GitHub feature/new-arch 分支）
3. 创建 Python venv + pip install
4. 安装 systemd 服务
5. 配置 nginx 反代
6. 申请 Let's Encrypt HTTPS 证书
7. 设置 Git pull + systemctl restart 的 cron 任务

### 11.5 数据库同步链路

| 端 | 操作 | 时间 | 工具 |
|---|---|---|---|
| Mac mini | 复制数据库 → git push | 每日 04:00 | `sync_db_to_ecs.sh` + cron |
| GitHub | 中转 | 实时 | — |
| ECS | git pull + restart API | 每小时 | systemd cron |

**响应延迟**：数据更新从 Mac mini 到 API 可用最长 1 小时。

---

## 十二、实时性策略（v3.1新增）

### 12.1 当前 CDN 缓存策略

| 页面 | 缓存策略 | 强制刷新方式 |
|------|---------|------------|
| Vercel 主站 | 默认缓存 600 秒（10分钟）| URL `?v=YYYYMMDD` 时间戳 |
| GitHub Pages 预览站 | 默认缓存 5 分钟 | URL `?t=cacheBuster` |
| API 接口 | 无缓存（实时返回）| — |

### 12.2 数据版本号机制

- 数据文件命名：`data.v{N}.json`（N 从 1 开始自动递增）
- 前端引用：`fetch('data.v76.json?v=20260825')`
- 更新时改两处：版本号 + URL 时间戳

### 12.3 待优化项

| 优化 | 效果 | 工作量 |
|------|------|--------|
| Vercel 边缘缓存改 60 秒 | 刷新即生效 | 5分钟 |
| 后端API直接读数据库（不生成JSON） | 实时 | 1-2天 |
| WebSocket 推送变更 | 推送即生效 | 3-5天 |

---

## 十三、关键运维命令

### SSH 连接
```bash
ssh root@8.133.238.76
# 密码：Clarins202020
```

### 服务管理
```bash
systemctl status cosmetic-api   # 查看状态
systemctl restart cosmetic-api  # 重启
journalctl -u cosmetic-api -f   # 实时日志
```

### 数据更新（手动触发）
```bash
# Mac mini 端
bash /Users/yuming.chen/.openclaw/workspace/cosmetic-deploy/api-server/sync_db_to_ecs.sh

# ECS 端（立即拉取）
cd /opt/cosmetic-api && git pull && systemctl restart cosmetic-api
```

### 健康检查
```bash
curl https://api.cosmetic-search.com/api/health
curl https://api.cosmetic-search.com/api/stats
```

---

## 十三、定制化深度报告表单设计（v3.2新增）

### 13.1 设计背景与原则

**背景**：08-19全开放式表单过于宽泛，交付不可控，返工风险高。

**三大设计原则**（Nick 2026-08-26拍板）：
1. **来源用户需提供**：依据内容不限于本站收录，但用户必须提供来源
2. **输入可控不宽泛**：用户可输入定制化想法，但不能太宽泛导致结果不满足需求
3. **明确验收标准**：让用户明确在意的点，但不能像六层表单那么夸张

### 13.2 三层渐进式表单结构

```
第一层：基础信息
├── 报告主题（必填）
├── 依据内容来源（必填，提示"不限于本站收录"）
└── 提示：支持多个链接，每行一个；或详细描述资料内容

第二层：需求描述
├── 我希望报告包含...（必填，3-5个要点引导）
│   └── 占位符示例：竞品对比/价格趋势/消费者决策因素
├── 报告用于...（单选：内部汇报/客户提案/竞品分析/趋势研究/其他）
└── 重点关注...（多选：成分/品牌/渠道/政策/技术/其他）

第三层：验收标准
├── 交付形式（单选：要点清单/段落概述/对比表格/完整报告）
├── 篇幅期望（单选：1-2页/3-5页/5-10页/不限）
└── 必须包含（必填，验收标准）
    └── 提示：竞品对比表格、数据支撑来源、趋势判断依据等

联系信息（最后）
├── 称呼（必填）
├── 邮箱（必填）
├── 微信/电话（选填）
└── 补充说明（选填）
```

### 13.3 字段设计说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| title | text | ✅ | 报告主题/标题 |
| source_content | textarea | ✅ | 依据内容来源（链接/文本描述） |
| report_points | textarea | ✅ | 我希望报告包含...（3-5要点） |
| use_case | radio | ✅ | 报告用于...（internal/client/competitor/trend/other） |
| focus_areas | checkbox | ⬜ | 重点关注...（JSON数组） |
| delivery_format | radio | ✅ | 交付形式（bullet/paragraph/table/full） |
| page_length | radio | ✅ | 篇幅期望（1-2/3-5/5-10/unlimited） |
| must_include | textarea | ✅ | 必须包含（验收标准） |
| name | text | ✅ | 称呼 |
| email | email | ✅ | 邮箱 |
| contact | text | ⬜ | 微信/电话 |
| notes | textarea | ⬜ | 补充说明 |

### 13.4 与智能选文报告的分工

| 功能 | 智能选文报告 | 定制化深度报告 |
|------|-------------|---------------|
| 报告类型 | 事实输出、综合概述 | 深度定制、非标需求 |
| 内容来源 | 本站已收录文章 | 用户自备（不限于本站） |
| 用户输入 | 勾选文章+选择选项 | 开放式描述+结构化引导 |
| 交付可控性 | 高（标准化） | 中高（验收标准明确） |

### 13.5 数据库迁移

Supabase `service_requests` 表新增8个字段，迁移脚本：`supabase_migration_service_requests.sql`

**迁移执行记录**（2026-08-26）：
- 使用 Management API + PAT（sbp_40c…82fa4）执行 ALTER TABLE
- 8个字段全部添加成功：title/source_content/report_points/use_case/focus_areas/delivery_format/page_length/must_include
- 状态：✅ 已完成

---

## 十四、数据库结构与LLM精排技术细节（v3.2新增）

### 14.1 数据库文章结构（13个字段）

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | INTEGER | ✅ | 主键，自增 |
| `wechat_name` | TEXT | ✅ | 公众号名称（18个白名单） |
| `title` | TEXT | ✅ | 文章标题 |
| `url` | TEXT | ✅ | 文章链接（唯一约束） |
| `publish_date` | TEXT | ⬜ | 发布日期（YYYY-MM-DD） |
| `content` | TEXT | ⬜ | 文章全文（部分文章为空） |
| `keywords` | TEXT | ⬜ | 关键词（10-15个，LLM生成） |
| `created_at` | TIMESTAMP | ⬜ | 入库时间（默认当前时间） |
| `content_html` | TEXT | ⬜ | HTML格式内容 |
| `images_json` | TEXT | ⬜ | 图片JSON数组 |
| `image_count` | INTEGER | ⬜ | 图片数量（默认0） |
| `table_count` | INTEGER | ⬜ | 表格数量（默认0） |
| `summary` | TEXT | ⬜ | 摘要（200-400字，LLM生成） |
| `structured_content` | TEXT | ⬜ | **结构化内容（新增，100%覆盖）** |

### 14.2 structured_content 字段结构（JSON）

```json
{
  "macro_narrative": "文章总体叙述走向（宏观脉络）",
  "micro_nodes": ["关键节点1", "关键节点2", "关键节点3", "关键节点4", "关键节点5"],
  "main_topic": "主要主题",
  "secondary_topics": ["次要主题1", "次要主题2"],
  "key_entities": ["核心实体1", "核心实体2", "核心实体3"]
}
```

**示例**（ID 1872 - 已使用化妆品原料目录第五次动态调整）：
- **macro_narrative**: 从监管机制解读→按时间顺序梳理五次动态调整→企业合规应对建议
- **micro_nodes**: 国家药监局完成第五次调整、纳入机制、五次调整具体内容等9个节点
- **main_topic**: 《已使用化妆品原料目录》第五次动态调整及五次调整全面梳理
- **key_entities**: 国家药监局、瑞旭集团、NMN、补骨脂酚等

### 14.3 LLM精排技术细节（意图锚定检索）

#### 整体架构

```
用户查询
    ↓
【步骤1】LLM意图解析（llm_parse_intent）
    ↓
【步骤2】锚点检索（anchor_search）—— FTS5 + BM25
    ↓
【步骤3】LLM精排（llm_rerank）—— relevance 0-100评分
    ↓
返回结果（含匹配理由）
```

#### 步骤1：LLM意图解析

**输入**：用户查询（如"抗衰老成分趋势"）

**LLM输出**：
```json
{
  "anchor": "抗衰老",
  "anchor_synonyms": ["抗老", "抗皱", "紧致", "提升", "年轻化", "冻龄", "逆龄"],
  "modifiers": ["成分", "趋势"],
  "exclude_if": ["美白为主", "保湿为主", "祛痘为主", "节日祝福", "企业新闻"]
}
```

**关键设计**：
- **anchor**：查询的核心概念，文章必须**主要讨论**这个概念
- **anchor_synonyms**：同义词/近义词，用于扩大召回
- **modifiers**：修饰概念，用于排序加权但不作为必须条件
- **exclude_if**：如果文章主要讨论这些主题，即使提及anchor也应排除

#### 步骤2：锚点检索（AND逻辑）

**FTS5查询构建**：
```sql
-- 锚点词组AND，修饰词可选
(anchor OR 同义词1 OR 同义词2 ...) AND (修饰词1 OR 修饰词2 ...)
```

**示例**（"抗衰老成分趋势"）：
```sql
("抗衰老" OR "抗老" OR "抗皱" OR "紧致" OR "提升" OR "年轻化" OR "冻龄" OR "逆龄") 
AND ("成分" OR "趋势")
```

**回退机制**：
- 如果AND查询结果 < 5篇，回退到仅锚点查询（去掉修饰词条件）

**时间过滤**：
- 解析查询中的时间词（"近一周"、"近一个月"等）
- 添加 `publish_date >= ?` 条件

#### 步骤3：LLM精排（核心创新）

**输入**：全部召回文章（无上限）

**动态并发策略**：
```python
def calculate_concurrency(total_articles: int) -> int:
    """根据召回数量计算最优并发数，目标30秒左右"""
    if total_articles <= 10:
        return 1   # 1批 × 10篇 = ~25秒
    elif total_articles <= 20:
        return 2   # 2批 × 10篇 = ~25秒
    elif total_articles <= 40:
        return 4   # 4批 × 10篇 = ~25秒
    elif total_articles <= 80:
        return 8   # 8批 × 10篇 = ~25秒
    elif total_articles <= 160:
        return 16  # 16批 × 10篇 = ~50秒（2轮）
    else:
        return 32  # 32批 × 10篇 = ~75秒（3轮+）
```

**全结构输入**（每篇约800字符，不看全文）：
```python
def build_article_text(article: dict, index: int) -> str:
    """构建单篇文章的完整输入文本"""
    # 标题（完整）
    # 主要主题（完整）
    # 宏观叙述（300字符）
    # 微观节点（前3个，200字符）
    # 核心实体（前5个，100字符）
    # 关键词（100字符）
    # 摘要（300字符）
```

**LLM Prompt**：
```
用户搜索: "抗衰老成分趋势"
核心意图锚点: "抗衰老"

请判断以下每篇文章是否**主要讨论**"抗衰老"这个概念。

判断标准：
1. 文章的主题必须是"抗衰老"——不能只是简单提及
2. 如果文章主要讲其他主题，即使提到"抗衰老"，也应排除
3. 以下情况必须排除：
   - 美白为主
   - 保湿为主
   - 祛痘为主
   - 节日祝福
   - 企业新闻

【文章1】ID:123
标题: 雅诗兰黛集团最新应用的tFNA，下一个成分新王？
主要主题: tFNA成分在雅诗兰黛集团的抗衰老应用
宏观叙述: 从tFNA成分机制→修复DNA损伤→促进自噬→基因级抗衰→产品应用
微观节点: tFNA核酸四面体结构、修复DNA损伤机制、促进细胞自噬
核心实体: tFNA、雅诗兰黛、DNA修复、自噬
关键词: tFNA,抗衰老,雅诗兰黛,DNA修复,自噬,成分创新
摘要: 文章以核酸四面体tFNA成分为核心，重点阐述其修复DNA损伤...

【文章2】ID:456
...

请严格按以下JSON格式返回：
{
  "selected": [
    {
      "id": 文章ID, 
      "relevance": 85, 
      "reason": "选择这篇文章的具体原因，用自然语言描述文章中哪些内容与抗衰老相关，不要提及技术字段名（如标题/主题/叙述/节点/实体/关键词/摘要等），直接说文章内容本身"
    },
    ...
  ]
}

注意：
- relevance范围0-100，表示文章与"抗衰老"的相关程度
- 只保留relevance >= 60的文章
- reason用自然语言描述文章内容与抗衰老的关系，例如"文章介绍了雅诗兰黛集团最新应用的tFNA成分，该成分通过修复DNA损伤和促进自噬实现抗衰老效果"
- 不要提及"标题"、"主题"、"摘要"、"关键词"等技术字段名
- 宁可少返回，也不要返回不相关的
```

**输出**：
```json
{
  "selected": [
    {
      "id": 123, 
      "relevance": 95, 
      "reason": "文章系统梳理了2026年抗衰老成分的最新趋势，包括NMN、补骨脂酚等核心成分的技术突破"
    },
    {
      "id": 456, 
      "relevance": 88, 
      "reason": "文章从皮肤光学视角解析皮肤衰老变黄的机制，涉及黑色素、糖化胶原等抗衰老关键因素"
    }
  ]
}
```

**排序**：按 `relevance` 降序排序，返回所有 relevance >= 60 的文章（不限制数量，无fallback）

#### 匹配理由生成

**优先级**：
1. **LLM语义理由**（`llm_reason`）—— 如果LLM返回了具体理由且不含"相关"关键词，直接使用
2. **模板生成** —— 基于anchor和modifiers生成

**示例**：
- LLM理由："本文系统梳理了2026年抗衰老成分的最新趋势，包括NMN、补骨脂酚等核心成分的技术突破"
- 模板理由："本文主要讨论「抗衰老」，同时涉及「成分」「趋势」，与您的搜索高度匹配。"

#### 关键指标

| 指标 | 数值 |
|------|------|
| LLM意图解析耗时 | ~10秒 |
| FTS5锚点检索耗时 | < 1秒 |
| LLM精排耗时 | ~25-50秒（动态并发，全部召回文章） |
| **总耗时** | **30-60秒**（目标30秒左右，召回≤80篇） |
| 召回数量 | 无上限（全部召回） |
| 精排后保留 | 所有 relevance >= 60 的文章（无fallback，LLM必须给出具体理由） |
| 并发策略 | 动态调整（1/2/4/8/16并发） |
| 输入内容 | 标题+主题+叙述+节点+实体+关键词+摘要（不看全文） |

#### 与四层关键词分层的区别

| 维度 | 四层关键词分层（旧） | 意图锚定检索（新） |
|------|---------------------|-------------------|
| 核心概念地位 | 平铺扩展，无特殊地位 | **锚点必须命中** |
| 召回逻辑 | 35个词OR召回 | 锚点AND召回 + 修饰词加权 |
| 排除机制 | 硬编码排除规则 | LLM判断 + exclude_if |
| 相关度评分 | 固定90分 | LLM动态评分0-100 |
| 匹配理由 | 关键词匹配 | LLM语义分析 |

**核心优势**：意图锚定确保文章**主要讨论**核心概念，而非简单提及，大幅提升检索精度。

---

## 十五、问题与改进记录（v3.2新增）

### 15.1 记录机制

**目的**：形成可追溯的问题解决知识库，避免同类问题重复发生。

**记录内容**：
1. **问题现象**：用户反馈/截图/日志
2. **根因排查过程**：调试日志/代码分析/日志分析
3. **真正根因**：技术细节
4. **修复措施**：代码修改/配置调整
5. **验证结果**：修复后效果

**记录位置**：CHANGELOG.md新增"问题与改进"章节，设计文档同步更新。

### 15.2 问题记录示例

#### 问题：LLM精排理由为空/模板化（2026-08-26，二次修复）

**问题现象**：
- 部分文章显示"本文涉及「抗衰老」相关内容。"（模板文本）
- 相关度显示84%但理由为空
- 耗时1分多钟（超过目标30秒）

**根因排查过程**：
1. 首次修复：前端字段名不匹配（后端`llm_reason` vs 前端`match_reason`），修复后仍有问题
2. 二次排查：后端`generate_match_reason()`函数过滤"相关"关键词
3. 代码审查：发现`'相关' not in llm_reason`过滤条件将LLM理由替换为模板文本

**真正根因**：
- `generate_match_reason()`函数中，如果`llm_reason`包含"相关"关键词，则使用模板文本覆盖
- 这导致LLM返回的自然语言理由（如"文章介绍了..."）被替换为"本文涉及「抗衰老」相关内容。"

**修复措施**：
```python
# 修复前
def generate_match_reason(article: dict, intent: Dict) -> str:
    llm_reason = article.get('llm_reason', '')
    if llm_reason and '相关' not in llm_reason:  # 问题：过滤"相关"关键词
        return llm_reason
    # ... 模板生成逻辑

# 修复后
def generate_match_reason(article: dict, intent: Dict) -> str:
    llm_reason = article.get('llm_reason', '')
    if llm_reason:  # 直接返回，不再过滤
        return llm_reason
    return f"本文涉及「{anchor}」相关内容。"
```

**验证结果**：
- 修复后显示LLM返回的自然语言理由
- 示例："文章介绍了雅诗兰黛集团最新应用的tFNA成分，该成分通过修复DNA损伤和促进自噬实现抗衰老效果"

**提交记录**：
- 主仓：commit 06236bf
- 预览仓：commit 06236bf

---

*文档生成时间：2026-08-26*
*基于v3.1（2026-08-25）更新，新增：定制化深度报告三层渐进式表单设计、数据库结构与LLM精排技术细节、问题与改进记录机制*
*反映截至2026-08-26 的系统实际运行状态*